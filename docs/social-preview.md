# תצוגה מקדימה בשיתוף — תגיות og: לדפי הפירוט

קישור לנכס שסוכן/ת שולח/ת ללקוח/ה בוואטסאפ הופיע בשיחה **ככתובת עירומה**:
בלי תמונה, בלי כותרת, בלי מחיר. אותו דבר בפייסבוק, בטלגרם ובתצוגה המקדימה
של גוגל. בשוק שבו וואטסאפ הוא ערוץ ההפצה העיקרי, זה מחיר ישיר על כל שיתוף.

## למה זה קרה

`property.html`, `agency.html` ו-`agent.html` הם קבצים סטטיים. הכותרת בהם
היא `נכס | שוק נדל״ן`, והתוכן האמיתי נטען ב-JS **אחרי** שהדף כבר בדפדפן.

הסורק של וואטסאפ אינו מריץ JS. הוא מושך את ה-HTML, מחפש תגיות `og:` ולא
מוצא אף אחת. הוספת התגיות לקובץ הסטטי לא הייתה עוזרת: הן היו זהות לכל
הנכסים.

## הפתרון

`netlify/edge-functions/og-tags.ts` רצה על ה-edge לפני שהתשובה יוצאת,
מושכת את הרשומה מ-Supabase, ומזריקה ל-`<head>` את `og:`, `twitter:`,
`description` ו-`canonical` — ומחליפה גם את ה-`<title>`.

ה-HTML לא משתנה בשום דרך אחרת, וה-JS שבדף ממשיך לעבוד כרגיל.

הפונקציה מוגדרת **בתוך הקובץ** (`export const config`), ולכן אין צורך
ב-`netlify.toml`. הנתיבים רשומים בשתי הצורות — `/property` ו-
`/property.html` — כי Netlify מגיש כל דף בשתיהן.

| דף | מפתח | מקור |
| --- | --- | --- |
| `/property` | `?id=` | `properties` (רק `status=active`) |
| `/agency` | `?slug=` | `agencies` |
| `/agent` | `?slug=` | `agency_members_public` |
| `/project` | `?slug=` | `projects_public` |
| `/developer` | `?slug=` | `developers_public` |
| `/professional` | `?slug=` או `?id=` | `professional_cards_public` |
| `/article` | `?slug=` או `?id=` | `articles_public` |

## שני סוגי דפים, ולמה ההזרקה שונה בהם

נכס, משרד וסוכן/ת אינם נושאים תגיות `og:` כלל, ולכן הן נכתבות מאפס.

פרויקט, חברה יזמית, בעל/ת מקצוע וכתבה **כן** נושאים אותן — עם ערכים
כלליים ועם `id`
(`ogTitle`, `metaDescription`, `ogDescription`, `ogImage`), כי ה-JS שלהם
ממלא אותן אחרי הטעינה (מה שלא עוזר לסורק, שאינו מריץ JS). שם התפקיד הוא
**למלא את התגיות הקיימות ולא להוסיף חדשות**, משתי סיבות:

1. תגית `og:title` כפולה משאירה לסורק לבחור — והוא בוחר את הכללית.
2. ה-JS בדפים האלה כותב `el('ogTitle').content = …` **בלי בדיקת null**.
   הסרת התגית הייתה זורקת `TypeError` באמצע `renderMeta` ושוברת את כל מה
   שבא אחריה — כלומר ההזרקה לסורק הייתה משלמת בדף של הגולש/ת.

`og:type` נשאר כפי שהדף כתב אותו (`article` לכתבה, `profile` לבעל/ת
מקצוע), ונכתב כ-`website` רק בדפים שאין בהם אחד.

**הכותרת והתיאור נבנים בשרת באותו כלל בדיוק שבו `renderMeta` שבדף בונה
אותם.** שינוי בצד אחד מחייב את אותו שינוי בשני — כמו `maskHouseNumber` —
אחרת הסורק והגולש/ת רואים שני דברים שונים.

## `canonical` — כתובת אחת לכל דף

`professional` ו-`article` מקבלים גם `?slug=` וגם `?id=`: קישורים ישנים,
ופרופיל שעוד לא נשמר פעם אחת ולכן אין לו `slug`. שתי הכתובות מגישות את
אותו תוכן, וזה בדיוק מה שגוגל קוראת לו תוכן משוכפל.

לכן ה-`canonical` נבנה **מה-`slug` שחזר מהמסד**, ולא מהפרמטר שבכתובת:
פנייה ב-`?id=` מצהירה על `?slug=` כמקור. זו גם הכתובת היחידה שנכנסת
ל-`sitemap` (`docs/sitemap.md`), ולכן השתיים מסכימות.

## ודף פירוט שאין מאחוריו רשומה - `noindex`

דף בלי `canonical` הוא מצב תקין כשהוא נדיר. אבל יש מקרה שבו הוא חוזר על
עצמו בהרבה כתובות בבת אחת, וכולן מגישות **בדיוק אותו HTML**:

| הכתובת | מה הגולש/ת רואה |
| --- | --- |
| `/property` בלי `?id=` | "הנכס לא נמצא" |
| `/property?id=` של נכס שנמכר ואינו `active` | "הנכס לא נמצא" |
| `/agency?slug=` שנמחק, `/article?slug=` שאינו קיים | "לא נמצא" |

שבעת דפי הפירוט מציגים אז את אותו שלד, בלי `canonical` - כי תגית סטטית
אסורה בהם (למעלה) ואין מה להזריק. גוגל רואה אשכול כתובות זהות שאף אחת
מהן לא הצהירה על מקור, ושמו בדוח הוא **"עותק משוכפל בלי שנבחר דף קנוני
על ידי המשתמש"**.

התשובה כאן אינה `canonical` אלא `<meta name="robots" content="noindex,follow">`,
והיא אומרת את מה שנכון באמת: אין כאן דף לאנדקס. `canonical` על
`/property` היה מאחד אליו כתובות של נכסים אמיתיים ברגע שהבירור נכשל -
כלומר בדיוק הכשל שהסעיף הקודם נבנה כדי למנוע. `follow` נשאר, כדי
שהפוטר בדף עדיין יוביל לסריקה.

### וההפרדה ששומרת על זה: "אין רשומה" מול "לא ידענו"

‏`sbFetch` מחזירה **שלוש** תשובות ולא שתיים:

| התשובה מ-Supabase | הסיווג | מה קורה לדף |
| --- | --- | --- |
| שורה | `row` | מוזרקות התגיות וה-`canonical` |
| `406` (‏`PGRST116`), `404` | `missing` | `noindex,follow` |
| `401`, `403`, `5xx`, `timeout`, JSON פגום | `error` | יוצא **כמו שהוא**, כמו קודם |

השורה האחרונה היא כל העניין. מי שיאחד אותה עם השנייה - `if (!res.ok)`
אחד במקום סיווג לפי סטטוס - יקבל `noindex` על **כל דפי הפירוט באתר**
בכל דקה שבה Supabase איטי או שמפתח הוחלף. שום דבר לא ייראה שבור: הדפים
ייטענו, הגולשים לא ירגישו, וגוגל תוציא אותם מהאינדקס בשקט.

ולכן ההחלטה הזו נבדקת ולא נקראת:

```sh
node --experimental-strip-types scripts/og_tags_test.ts
```

‏19 מקרים מול מסד מדומה - דף בלי מפתח, `406`, `404`, `401`, `403`, `500`
ושורה תקינה - והם רצים ב-CI יחד עם `check_canonical.py` (‏`canonical.yml`).

## שלוש החלטות שחשוב להבין

### מפתח anon, לא service_role

הפונקציה קוראת עם אותו מפתח ציבורי שהדפדפן משתמש בו, ולכן **RLS חל עליה
כרגיל**. נכס שאינו `active` פשוט לא יימצא, והדף יוגש עם התגיות הכלליות.
אין כאן שום הרשאה שלא הייתה קודם.

### מספר הבית ממוסך — וזה לא פרט

`docs/property-address-privacy.md` קובע שדף הנכס מציג רחוב ושכונה ולא
כתובת מלאה, ואומר במפורש שהכלל חל גם על **מה שנראה בשיתוף**.

`maskHouseNumber` בקובץ הזה היא העתק מדויק של זו שב-`property.html`.
בלעדיה הפונקציה הייתה מדליפה את מספר הבית לכל שיתוף בוואטסאפ — כלומר
הופכת את כלל הפרטיות לאות מתה, דרך הדלת האחורית.

המיסוך מוחק **מספר ולא מילה**: הוא דורש גבול ספרה משני הצדדים, כך
ש-`20` יורד מהכותרת אבל `120 מ״ר` שבאותה שורה נשאר. זה נבדק מול נכסים
אמיתיים מהמסד.

> **שינוי ב-`maskHouseNumber` באחד משני הקבצים מחייב את אותו שינוי בשני.**

### כישלון אינו שובר דף

כל קריאת רשת עטופה ב-`AbortController` עם תקרה של 1.2 שניות וב-`try/catch`,
וכל מסלול כושל מחזיר את התשובה המקורית כמו שהיא. דף בלי תגיות שיתוף הוא
בדיוק מה שהיה קודם; דף שנתקע בגלל Supabase איטי הוא הרעה.

## מטמון

התשובה נשמרת ב-CDN של Netlify לדקה (`stale-while-revalidate` לחמש), כדי
שלא תצא קריאה ל-Supabase בכל צפייה. הדפדפן מקבל `must-revalidate`, ולכן
אין סיכון להגשת דף ישן אחרי פרסום.

הפונקציה מעתיקה את כותרות התשובה המקורית (`new Headers(res.headers)`)
לפני שהיא משנה את המטמון — ולכן **כל כותרות האבטחה מ-`_headers` נשמרות**.

## בדיקה אחרי פרסום

התגיות לא נראות ב-"הצג מקור" של הדפדפן אם הוא מקבל תשובה מהמטמון שלו.
הדרך לראות מה הסורק רואה:

```sh
curl -s "https://shuknadlan.co.il/property?id=<id>" | grep -E 'og:|<title>'
```

ולתצוגה המקדימה עצמה: מנפה השיתוף של פייסבוק
(`developers.facebook.com/tools/debug/`) מציג בדיוק מה שנקרא, ומאפשר
לרענן את המטמון שלהם אחרי תיקון.

## מה מכוסה, ומה מחזיק את זה ככה

כל שבעת דפי הפירוט מכוסים, וכולם נמצאים גם ב-`sitemap`
(`netlify/edge-functions/sitemap.ts`, ‏`docs/sitemap.md`) — כלומר מה
שגוגל תמצא היא גם תוכל לקרוא נכון.

`/developer` היה הסוג שנשמט: דף ציבורי שלם שלא היה כאן, לא ב-`sitemap`
ולא בקובץ הסטטי. הוא נוסף, ו-`scripts/check_canonical.py` מוודא מעכשיו
שכל סוג פירוט רשום **בשתי** הפונקציות — כי סוג חסר אינו שובר כלום, הוא
רק נעדר מגוגל.

ודפי הרשימות — `index`, `agencies`, `agents`, `articles` וחבריהם — נושאים
`canonical` סטטי משלהם ב-`<head>`, ולא צריכים שרת בשביל זה: הכתובת שלהם
קבועה. בעקבות זאת ירדו מ-`robots.txt` שלוש שורות החסימה של פרמטרי הסינון,
שהיו שם במקום התגית וגרמו לנזק שניסו למנוע. `docs/security-headers.md`.
