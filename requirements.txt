# מנוע הלידים מפידי RSS (scraper.py)
anthropic>=0.69,<2
supabase>=2.15,<3
feedparser>=6.0.11,<7
requests>=2.32,<3
pydantic>=2.9,<3
python-dotenv>=1.0,<2

# הקובץ הזה משרת את שלושת המנועים שרצים ב-Actions: מנוע הלידים, מנוע
# המבזקים ומנוע העסקאות הרשמיות (deals_scraper.py). לאחרון אין תלות
# חדשה — requests ל-nadlan.gov.il ו-supabase לכתיבה, שניהם כבר כאן,
# והוא אינו משתמש ב-anthropic.
#
# לסוכן התפעולי יש קובץ משלו (ops_agent/requirements.txt) — האתר אינו
# מריץ פייתון, ואין סיבה שדרייבר של Postgres ישב כאן.
