# מנוע הלידים מפידי RSS (scraper.py)
anthropic>=0.69,<2
supabase>=2.15,<3
feedparser>=6.0.11,<7
requests>=2.32,<3
pydantic>=2.9,<3
python-dotenv>=1.0,<2

# ⚠️ **הקובץ הזה נטען גם בבנייה של Netlify.** תמונת הבנייה שלה מריצה
# ‏`pip install -r requirements.txt` מעצמה כשהקובץ קיים בשורש, ולכן חבילה
# שאין לה wheel מתאים שם מפילה את **פריסת האתר** — לא רק סקריפט.
# תלות שנחוצה רק לכלי שרץ ב-Actions הולכת לקובץ requirements משלה
# (ראו ops_agent/requirements.txt), ולא לכאן.
