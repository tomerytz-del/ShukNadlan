# תלויות הסוכן התפעולי (ops_scan.py, docs/ops-agent.md).
#
# **למה קובץ נפרד ולא requirements.txt שבשורש:** הקובץ שבשורש משרת את שני
# המנועים שרצים ב-Actions, והאתר אינו מריץ פייתון בכלל. אין סיבה שדרייבר
# של Postgres ישב שם, וההפרדה גם מקצרת את ההתקנה ב-workflow.
#
# ‏(הנימוק המקורי כאן טען שהקובץ בשורש מותקן בבנייה של Netlify ושזה מה
# שהפיל דיפלוי. זה היה שגוי — ההסבר האמיתי ב-docs/ops-agent.md.)
#
# הסוכן מדבר עם המסד ב-SQL ישיר ולא דרך PostgREST: השאילתות שלו נוגעות
# ב-pg_stat_statements, ב-pg_policies וב-cron.job_run_details — קטלוגים
# שלקוח ה-supabase אינו חושף כלל.
psycopg2-binary>=2.9,<3

# לבדיקות הזמן החי מול האתר ולבריאות ה-workflows
requests>=2.32,<3
