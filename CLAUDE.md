# הנחיות לעבודה בריפו הזה

## סקילים

חמישה תהליכים בריפו הזה מכניים, קלים לטעות, והטעות בהם שקטה. לכל אחד יש
סקיל ב-`.claude/skills/` שמרכז את הכללים ואת הסיבות שמאחוריהם:

| סקיל | מתי |
| --- | --- |
| `new-page` | דף HTML חדש בשורש — GTM, `events.js`, אימות גוגל, בריחה, `robots`/`sitemap` |
| `new-migration` | כל שינוי סכימה — שם קובץ, אידמפוטנטיות, הרשאות, ואיסור DDL מחוץ לצינור |
| `new-edge-function` | פונקציה חדשה — `verify_jwt` ב-`config.toml`, CORS, סודות |
| `new-tier-capability` | יכולת שתלויה במסלול — הגייט במסד, השורה ב-`pricing.html`, והשורה בתיעוד |
| `perf-measure` | כל טענה על ביצועים — השרת שמחקה את Netlify, שני התרחישים, והמוקשים |

> ‏**שימו לב:** הטבלה הזו, וכל הערות הקוד בריפו, משתמשות במקף ארוך —
> וזה בסדר. האיסור שלמטה חל על **טקסט שהאתר מציג**, לא על התיעוד.

כללי שלושת הראשונים מופיעים גם כאן למטה; הסקילים הם מה שנטען כשעובדים
בפועל. ‏`perf-measure` הוא היוצא מן הכלל — הוא נושא גם **קוד להרצה**
(שרת וסקריפט מדידה), ולכן הוא חי רק שם.

## מפה קצרה

- **האתר** — קובצי HTML בשורש (`index.html`, `property.html`, `agency.html`, …)
  ועוזרים ב-`assets/`. ‏Netlify מפרסם אותם תוך שניות מהמיזוג ל-`main`.
  שתי פונקציות ב-`netlify/edge-functions/` משלימות בשרת את מה שדף סטטי
  אינו יכול לתת לסורק: `og-tags.ts` מזריקה תגיות שיתוף לדפי הפירוט
  (`docs/social-preview.md`), ו-`sitemap.ts` מוסיפה ל-`sitemap.xml` את
  הכתובות שנוצרות מהמסד (`docs/sitemap.md`).
- **המסד** — פרויקט Supabase `obookujgolazrwycsiyn`. הסכימה ב-
  `supabase/migrations/`, ה-Edge Functions ב-`supabase/functions/`. שניהם
  נפרסים ב-GitHub Actions בכל push ל-`main` שנוגע בהם.
- **המנועים** — `news_engine/` + `news_scraper.py` (מבזקי הנדל"ן),
  `lead_engine/` + `scraper.py` (לידים מ-RSS), ו-`deals_engine/` +
  `deals_scraper.py` (עסקאות מרשות המיסים לדוח ה-CMA), רצים ב-workflows
  מתוזמנים. למנוע העסקאות יש כלל משלו: **רשומה פגומה נזרקת ונספרת ולא
  מנוחשת**, כי עסקה שנכנסת למאגר מסומנת `official` — המספר שהדוח סומך
  עליו יותר מכל אחר. ‏`docs/market-deals-official.md`.
- **הסוכן התפעולי** — `ops_agent/` + `ops_scan.py`. סורק כל שש שעות את
  המסד, האתר החי, הקוד והצינור, וכותב ממצאים ל-`ops_findings`. שני
  הפאנלים שמציגים אותם — בריאות ומצבת — יושבים בתצוגת מנהל/ת הפלטפורמה
  ב-`crm.html`. **הסריקה היא קריאה בלבד ברמת המסד**
  (`default_transaction_read_only`), ולכן היא אינה יכולה להפר את כלל
  ה-DDL למטה גם בטעות. הפרטים: `docs/ops-agent.md`.
- **התיעוד** — `docs/*.md`, מסמך לכל יכולת. מי שנוגע ביכולת מעדכן את המסמך
  שלה באותו PR.

## דף HTML חדש: תגיות GTM הן חלק מהדף

כל דף באתר נמדד, ולכן **דף חדש בשורש נולד עם שני קטעי ה-GTM בתוכו** —
קטע ה-`<script>` ב-`<head>` מיד אחרי `<meta charset>`, וקטע ה-`<noscript>`
מיד אחרי `<body>`. הדרך הבטוחה היא להעתיק אותם מ-`index.html` כלשונם.

**למה זה משוכפל בכל דף ולא נטען ממקום אחד:** אין לאתר תבנית משותפת. כל דף
הוא קובץ HTML עצמאי ש-Netlify מפרסם כמו שהוא, ומכולה שנטענת מקובץ נפרד
נופלת יחד איתו אצל כל מי שיש לו חוסם פרסומות.

**למה שוכחים:** דף בלי התגיות נטען, נראה תקין, ומתנהג כרגיל. אין שגיאה
בקונסול ואין דבר שנשבר — הדף פשוט נעדר מהדוחות, ומגלים את זה חודש אחרי,
כשהנתונים של החודש הזה כבר אבודים ואי אפשר לשחזר אותם למפרע.

לכן `scripts/check_gtm.py` חוסם ב-CI כל PR שנוגע ב-HTML ומשאיר דף חסר.
אפשר להריץ אותו לפני הדחיפה:

```sh
python scripts/check_gtm.py
```

הפלט כשמשהו חסר מראה מה להדביק ואיפה. הפרטים: `docs/analytics-gtm.md`.

## דף HTML חדש: גם בלוק ה-PWA

מאותה סיבה בדיוק — אין תבנית משותפת — **דף חדש נושא גם את בלוק ההתקנה
כאפליקציה**: קישור למניפסט, `theme-color`, אייקון אפל, ו-
`assets/pwa-install.js`. הוא יושב ב-`<head>` מיד אחרי
`<link rel="icon">`, ומעתיקים אותו מ-`index.html` כלשונו.

דף באזור האישי (`crm`, `developer-crm`, `professional-manage`) מחליף שם
שורה אחת: `href="/app-crm.webmanifest"` במקום `/manifest.webmanifest`.
המניפסט הזה נפתח על `/crm` ולא על דף הבית — סוכן/ת שהתקין/ה מה-CRM
מצפה/ה להיפתח ב-CRM.

**למה שוכחים, ומה נשבר:** דף בלי הבלוק נטען ונראה תקין לחלוטין. מה שאין
בו הוא ההצעה להתקין, וגרוע מכך — מי שכבר התקין/ה והגיע/ה לדף הזה מתוך
האפליקציה עלול/ה לצאת ממנה החוצה לדפדפן באמצע.

```sh
python scripts/check_pwa.py
```

הבדיקה חוסמת ב-CI דף חסר ומניפסט הפוך. הפרטים, כולל הדרך להסביר לאייפון
ולבנות מחדש את האייקונים: `docs/pwa-install.md`.

## דף HTML חדש: גם תגית האימות של גוגל

ומאותה סיבה שוב — **דף חדש נושא גם את תגית האימות של Google Search
Console**, מיד אחרי בלוק ה-PWA, זהה בכל דף:

```html
<meta name="google-site-verification" content="_ALC1h51UGYFz3-d53Q9wYeB88QS5ow5DbJ-ZgV1PDA">
```

בלי אימות אין גישה לדוחות החיפוש, אין הגשת `sitemap` ואין בקשת אינדוקס.
גוגל בודקת את התגית שוב גם אחרי שהאימות עבר, ולכן מחיקה שלה **מבטלת
אימות קיים**.

שלושת דפי הפירוט הם המקרה שמסביר את הכלל: `property.html`,
`agency.html` ו-`agent.html` הם קובץ אחד שמגיש את **כל** הנכסים,
המשרדים והמתווכים באתר, ולכן תגית אחת בכל אחד מהם מכסה גם כל נכס
שייווצר מחר.

```sh
python scripts/check_search_console.py        # בדיקה
python scripts/check_search_console.py --fix  # שתילה בדף שחסר
```

הבדיקה חוסמת ב-CI דף חסר וטוקן שהתפצל. הפרטים, כולל רשומת ה-TXT ב-DNS
שאינה בריפו ואינה מכוסה בבדיקה: `docs/security-headers.md`.

## דף HTML חדש: מדידת אירועים

דף **ציבורי** חדש טוען בנוסף את `assets/events.js` מיד אחרי קטע ה-GTM,
אחרת לחיצות "וואטסאפ לסוכן" ו"חייגו" בו לא נמדדות. דפי האזור האישי
(`crm`, `developer-crm`, `professional-manage`) דווקא **לא** טוענים אותו:
שם סוכנים מתקשרים ללקוחות של עצמם, וזה היה נספר כפניות של גולשים.
הפרטים: `docs/analytics-events.md`.

## הקוד של הדף יוצא ל-`assets/`, העיצוב נשאר בתוכו

דף שצבר `<script>` מוטבע גדול מוציא אותו לקובץ ב-`assets/` — **את
ה-JS בלבד.** שני הדפים הכבדים באתר כבר עברו:

| דף | היה | הקובץ | הדף אחרי |
| --- | --- | --- | --- |
| `crm.html` | 1,533KB | `assets/crm.js` | 447KB |
| `index.html` | 606KB | `assets/home.js` | 326KB |
| `property.html` | 251KB | `assets/property.js` | 123KB |

**‏`<script src>` רגיל, לא `defer`, ובדיוק במקום שבו ישב המוטבע.** ‏`defer`
דוחה את הריצה לאחרי הסקריפטים ה-`defer` האחרים בדף (‏`pwa-install.js`,
‏`site-footer.js`), ושני הקבצים האלה נוגעים בגלובלים שמגיעים מהם. תג רגיל
באותו מיקום שומר על סדר ריצה **זהה**, וזו כל הנקודה: ההעברה חייבת להיות
בייט-בייט, בלי שום שינוי סמנטי.

### ‏ה-CSS נשאר מוטבע, וזו לא העדפה אלא מדידה

הוצאת ה-CSS של `crm.html` ל-`assets/crm.css` נוסתה, נמדדה **והוחזרה**:
הציור הראשון עלה מ-916ms ל-1,432ms. ‏`<link rel="stylesheet">` חוסם ציור
עד שהקובץ מגיע — RTT שלם נוסף — בעוד CSS מוטבע מגיע יחד עם תחילת המסמך.

### מה זה קונה, ומה לא

**בביקור החוזר הרווח ודאי. בביקור הראשון — תלוי בדף, ואין לנו כלל
שמנבא כמה.** שלושת הדפים נמדדו באותו מכשיר, תשע דגימות לכל גרסה, ונתנו
שלוש תשובות שונות ל-FCP בביקור ראשון:

| דף | המסמך ברשת | FCP בביקור ראשון |
| --- | --- | --- |
| `crm.html` | ‏404 → 105KB | ללא שינוי |
| `index.html` | ‏165 → 85KB | ‎-36ms‎ |
| `property.html` | ‏73 → 33KB | **‎-156ms‎** |

שימו לב לכיוון: דווקא הדף שהמסמך שלו קטן הכי מעט הרוויח הכי הרבה. **מה
שכן נשלל בבדיקה** הוא ההסבר המתבקש — שהסקריפט של `property.html` יושב
באמצע ה-body ולא בסופו. אותם בתים בדיוק, מוטבעים בסוף ה-body, נתנו FCP
זהה (‏984ms מול 984ms). המיקום אינו העניין, וההסבר עדיין לא בידינו.

לכן **מודדים כל דף**, ולא מניחים. הרווח שכן ודאי הוא בביקור שאחרי
פריסה, כי שינוי במבנה הדף כבר אינו גורר הורדה מחדש של כל הקוד:

| | ‏load | ירד מהרשת |
| --- | --- | --- |
| `crm.html` | ‏2,372ms → 953ms | ‏407KB → 109KB |
| `index.html` | ‏1,202ms → 995ms | ‏170KB → 90KB |
| `property.html` | ‏852ms → 829ms | ‏77KB → 38KB |

זה התרחיש השכיח כאן — רוב ה-PR-ים נוגעים במבנה הדף ולא בכל הקוד שבו.

**והמחיר, שנמדד גם הוא — בדף אחד מתוך שלושה:** בדף הבית הבקשה הנוספת
הוסיפה כ-30ms לציור הראשון בביקור החוזר (‏876ms → 908ms) והגדילה את
הפיזור. שלוש סדרות נתנו שם ‎-4‎, ‎+28‎ ו-‎+32ms‎ — אפקט קטן ואמיתי, ולא
רעש כפי שנראה מסדרה אחת. ב-`property.html`, לעומת זאת, גם הביקור החוזר
השתפר (‎-28ms‎). כלומר גם המחיר אינו קבוע, וגם אותו מודדים.

**מה שאסור להוציא נשאר בפנים:** קטעי ה-GTM, בלוק ה-PWA ותגית האימות של
גוגל — כל אחד מהם מהסיבה שכתובה בסעיף שלו למעלה.

**כינויי הבריחה נוסעים עם הקוד** (`function escAttr(s){ return
escapeHtml(s); }`), והם נשארים תקינים כי `assets/esc.js` הוא `<script src>`
רגיל שקודם להם בסדר המסמך. מודול ב-`assets/` שמגדיר עותק **משלו** עדיין
חייב את כל חמשת התווים — ראו הסעיף על בריחת HTML.

**מי שמוציא דף נוסף מודד אותו** — הסקיל `perf-measure` נושא את השרת, את
הסקריפט ואת המוקשים. המדידות שכבר נעשו: `docs/agent-dashboard.md`.

## טקסט שמוצג לגולשים: בלי מקף ארוך

**כל טקסט שהאתר מציג נכתב עם מקף רגיל (`-`) ולא עם מקף ארוך (`—` או
`–`).** זה חל על טקסט ב-HTML, על מאפיינים שמוצגים (`alt`, `title`,
`placeholder`, `aria-label`, `data-info`, ה-`content` של תגיות `meta`), ועל
מחרוזות ב-JS ובפונקציות ה-Edge.

**הערות קוד פטורות.** שם המקף הארוך נשאר, כי ההערות בריפו הזה הן התיעוד
של המפתחים ולא תוכן שמתפרסם.

**למה זה צריך בדיקה ולא רק שורה כאן:** מקף ארוך אינו שובר כלום. הדף נטען,
נראה תקין, אין שגיאה בקונסול — הוא פשוט נכתב בסימן שאינו שלנו. והוא נכנס
בלי כוונה: מקף ארוך הוא מה שעורכי טקסט ומודלים מייצרים כברירת מחדל, ולכן
**כל נוסח חדש נולד איתו** אלא אם מישהו עוצר אותו.

```sh
python scripts/check_long_dash.py        # בדיקה
python scripts/check_long_dash.py --fix  # החלפה למקף רגיל
```

הבדיקה חוסמת ב-CI. הניקוי הראשון החליף 1,616 מופעים ב-76 קבצים.

### ההפרדה נעשית במכונת מצבים, ולא ב-regex

וזה לא קפדנות: הניסיון הראשון כאן **שינה התנהגות**. ‏`regex` שמכיל גרש —
למשל ``/["'״׳]/g`` ב-`assets/property-card.js` — פתח "מחרוזת" מדומה, וכל
הקובץ שאחריו סווג הפוך. כך דווקא ה-regex-ים שמנקים מקפים הוחלפו:

```js
/^[\s,·—–-]+$/   →   /^[\s,·---]+$/
```

מחלקת התווים שנועדה לנקות מקפים ארוכים הפסיקה להכיר אותם, ו-`-` האמצעי הפך
לאופרטור טווח. הקוד עדיין מתקמפל, ה-regex עדיין תקין, והוא פשוט מפסיק
להתאים. לכן הסורק מזהה ליטרלים של regex ומסמן אותם `code` — ו-`code` לעולם
אינו מוחלף.

### וגם בטקסט שנוצר בזמן ריצה

בדיקת CI רואה קבצים, ולכן היא **אינה** מכסה את הטקסט שהמערכת כותבת בעצמה:
התיאור השיווקי והפוסט לפייסבוק (`marketing-description.md`,
`facebook-auto-publish.md`). שם הכלל נאכף בקוד:

| שכבה | איפה |
| --- | --- |
| סעיף בפרומפט | `SYSTEM_PROMPT` ב-`_shared/marketing-copy.ts` |
| ניקוי התוצאה | `noLongDash()` על `description` ועל `post` |
| ניקוי הפוסט המורכב | `buildMessage()` ב-`property-marketing-publish` |

**למה שתי שכבות ולא רק ההנחיה:** מקף ארוך הוא מה שמודל שפה מייצר כברירת
מחדל, והוא חוזר בדיוק כשהניסוח נעשה יפה יותר. הנחיה היא בקשה; הניקוי הוא
מה שהופך אותה לערובה.

**ולמה הניקוי גם ב-`buildMessage`:** הפוסט מורכב שם משלושה מקורות —
‏`post_text` שהמודל כתב, נוסח שסוכן/ת כתב/ה ביד, או שורה שנשמרה במסד לפני
שהכלל נולד — ועוד שורות מיקום, מפרט ומחיר שנבנות מנתוני המסד.

### והטקסט שכבר היה במסד

נוקה פעם אחת, למפרע, במיגרציה `20261202090000_long_dash_backfill.sql` —
‏119 שורות: כותרות נכסים, תיאורים שיווקיים, פוסטים לפייסבוק, מחירון,
כתבות, ביו ושמות מקורות חדשות.

**מה שלא נוגע בו שם, ולמה:** רשומות היסטוריות (‏`whatsapp_messages`,
‏`property_publications.message`, `notifications`) — לשכתב אותן זה לזייף
את היומן, כי הפוסט שעלה לפייסבוק עלה עם מקף ארוך. וכן תוכן פנימי
(‏`ops_findings`) וטקסט פרטי של משתמש/ת (‏`agent_clients.notes`).

**והמלכודת שנתפסה שם:** `property_marketing_fingerprint()` היא md5 של
‏`title` ו-`description` ועוד שדות. ניקוי תמים של הכותרת היה מסמן 50
נכסים כמתיישנים בבת אחת — 50 קריאות Claude מיותרות על שינוי של תו אחד.
המיגרציה אוספת מי היה טרי **לפני**, ומחזירה להם את טביעת האצבע.

**מה שנמדד אחרי ההרצה (20.9.2026):** אפס מקפים ארוכים בעשר העמודות
שמתפרסמות, ומצב ההתיישנות נשמר — אף נכס לא סומן מחדש בגלל הניקוי. האימות
נעשה מול המסד ולא מול הלוג, כי **`raise notice` במיגרציה אינו מגיע ללוג
של Actions**; הפרטים והנוהל: `docs/supabase-migrations.md`, כלל 0.

**ולמה לא נדרשה פריסה:** הטקסטים האלה נשלפים מהמסד בזמן ריצה — הדפדפן
ב-`assets/home.js`, ופונקציית הקצה `og-tags.ts` בכל בקשה — ולכן הם
התחלפו באתר באותה שנייה שבה המיגרציה נגמרה. היחיד שמשהה זאת הוא מטמון
ה-CDN: דקה ל-`og-tags`, שעה ל-`sitemap`.

## בריחת HTML: `escapeHtml` ולא הגדרה מקומית

דף שמכניס ערך מהמסד ל-`innerHTML` או למאפיין טוען את `assets/esc.js`
(מיד אחרי קטע ה-GTM, כמו `events.js`) ומשתמש ב-`escapeHtml` הגלובלית.
**אין להגדיר בריחה משלך בדף.**

הייתה לפונקציה הזו הגדרה נפרדת כמעט בכל דף, תחת שמונה שמות — `esc`,
`escAttr`, `escapeAttr`, `impEscape`, `escapeArticleText` ועוד — וחלקן
טיפלו ב-`& " <` בלבד, בלי גרש בודד. ערך כזה בתוך `attr='…'` יוצא ממנו
החוצה. אבל הנזק העיקרי היה אחר: **כששמונה שמות מתחרים, אין שם אחד
נכון, ובפועל לפעמים לא נקרא אף אחד.** כך נוצרה ההזרקה ב-`buildSearchRow`
בדף הבית — `escAttr` היה זמין באותו קובץ, שימש שתי שורות מעל לכתובות
התמונה, ובשדות הטקסט פשוט לא נשלף.

שם מקומי קיים נשאר ככינוי מפורש, ותמיד כ-`function` ולא כ-`const`:
`const` אינו מורם, וב-`crm.html` יש 65 אתרי קריאה שקודמים לשורת ההגדרה.

```js
function esc(s){ return escapeHtml(s); }
```

מודול ב-`assets/` שמגדיר עותק משלו עושה זאת בכוונה — סדר הטעינה בין
קבצי `assets` אינו מובטח — והעותק חייב לברוח מכל חמשת התווים.

`scripts/check_escapers.py` חוסם ב-CI הגדרה חדשה בדף ועותק חלקי ב-
`assets`. אפשר להריץ לפני הדחיפה:

```sh
python scripts/check_escapers.py
```

## נכס באוויר: סטטוס, כפילות ובלעדיות

מודעה עולה לאתר רק דרך `status='active'`, והמעבר הזה נשמר בטריגר
`properties_guard_duplicate`: **אותו נכס ואותם בעלים לא יכולים להיות
מפורסמים פעמיים.** מי שרוצה לפרסם נכס שכבר קיים במערכת חייב הסכם בלעדיות
חתום (`agreements`, ‏`kind = exclusive_*`, ‏`status = 'signed'`), ותובע
אותו דרך `claim_property_exclusivity()`.

שתי מלכודות:

1. **הטריגר צריך לדעת מי הבעלים, והם יושבים ב-`property_owners`** — טבלה
   שנכתבת רק אחרי שיש `property_id`. לכן נכס חדש ב-CRM נוצר כ-
   `unpublished`, הבעלים נשמרים, ורק אז הוא מתעדכן ל-`active`. מי שיחזיר
   את היצירה ל-`insert` יחיד עם `status='active'` יחסום מודעות תקינות
   (הדירה השכנה, אותה קומה ואותו מספר חדרים).
2. **`property_text_key()` במסד ו-`propNormText()` ב-`crm.html` חייבות
   לנרמל זהה.** נרמול שונה מציג "אין כפילות" בדפדפן ואז נופל בשמירה.

מה שלא נאמר למי שאינו בעל/ת הבלעדיות: **מתי היא נגמרת**. ההודעה נושאת שם
מתווך/ת ושם משרד בלבד. הפרטים: `docs/property-status-exclusivity.md`.

## מיגרציות: אין להחיל DDL מחוץ לצינור

**זה הכלל החשוב ביותר כאן, והוא כבר הופר פעם אחת ושבר את הפרודקשן.**

שינוי סכימה נכנס **רק** כקובץ ב-`supabase/migrations/`, שה-workflow
‏`supabase_migrations.yml` מריץ במיזוג ל-`main`. אסור להחיל DDL דרך
‏`apply_migration` של Supabase MCP, דרך הדשבורד, או דרך `psql` ידני — גם לא
"רק כדי לבדוק", וגם לא כשזה נראה כמו הדרך המהירה.

**למה זה שובר:** כל כלי כזה רושם את ההחלה ב-
`supabase_migrations.schema_migrations` תחת `version` של **רגע ההרצה**, ולא
תחת ה-version שבשם הקובץ. נוצרת רשומה שאין לה קובץ מקומי, ו-`db push` מסרב
לרוץ בגללה — ‏**לכל** המיגרציות שאחריה, גם החדשות לגמרי:

```
Remote migration versions not found in local migrations directory.
```

הכשל הזה ערמומי כי הסכימה עצמה נכונה: העמודה שהוחלה נמצאת שם, האתר עובד,
ורק הצינור מת. כך זה קרה ב-2.9.2026 (‏PR ‎#137, המיגרציה `news_story_key`
שנרשמה תחת `20260902221109` במקום `20260919090000`), וההרצה הבאה נפלה.

מותר וכדאי **לקרוא** מהמסד בזמן פיתוח — `execute_sql` עם `select`, לבדוק
סכימה, נתונים או שמות עמודות. הגבול הוא בכתיבת DDL.

### כשכותבים מיגרציה

1. שם הקובץ `YYYYMMDDHHMMSS_שם_קצר.sql`, וה-version חייב להיות ייחודי —
   ‏version כפול (קורה בין שני PR-ים מקבילים) מפיל את כל ה-job.
2. **אידמפוטנטית**: `add column if not exists`, `create or replace`,
   בדיקת `pg_constraint` לפני `add constraint`. היא עלולה לרוץ שוב.
3. ‏`create or replace view` — עמודה חדשה **בסוף** בלבד.
4. הקוד והמיגרציה באותו PR. זו כל הנקודה.

### אחרי מיזוג שכולל מיגרציה

מבט אחד ב-**Actions**. ‏workflow אדום שם הוא **תקלת פרודקשן, לא רעש CI**:
ה-HTML כבר באוויר, והסכימה לא. אם הרשומה שחוסמת נוצרה מהחלה מחוץ לצינור של
מיגרציה שקיימת אצלנו כקובץ — ה-workflow מיישר אותה בעצמו ומדווח באזהרה; בכל
מקרה אחר הוא נכשל ומדפיס את ה-DDL של הרשומה, כדי שההחלטה תהיה מבוססת.

הסיפור המלא, כולל כל הכשלים שכבר נתקלנו בהם: `docs/supabase-migrations.md`.
